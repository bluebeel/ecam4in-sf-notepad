<div class="col-lg-3">
    <h1 class="my-4">Category</h1>
    <div class="list-group">
        <a href="{{ path('notes') }}" class="list-group-item active">All</a>
        {% for item in categories %}
            <a href="{{ path('category_show', {'id': item.getId() }) }}" class="list-group-item">{{ item.getLibelle() }}</a>
        {% endfor %}
    </div>
</div>